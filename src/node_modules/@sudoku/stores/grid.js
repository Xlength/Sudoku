/**
 * 网格模块 - 向后兼容的导出
 * 
 * 此文件已重构为三个独立模块：
 * - puzzle-grid.js: 题目网格管理
 * - user-grid.js: 用户输入网格管理
 * - grid-validator.js: 验证逻辑管理
 * 
 * 此文件保持向后兼容，重新导出拆分后的模块
 */

// 导入拆分后的模块
import { puzzleGrid } from './puzzle-grid.js';
import { userGrid } from './user-grid.js';
import { createGridValidator } from './grid-validator.js';

// 创建验证器实例
const gridValidator = createGridValidator(userGrid);

// 向后兼容导出
export const grid = puzzleGrid;
export { userGrid };
export const invalidCells = gridValidator;